#include <iostream>
using namespace std;
template <typename B>

B maxnumber(B a, B b)
{
    if (a > b)
        return a;
    else
        -+d return b;
}

int main()
{
    cout << " THe max number is " << maxnumber(20, 30);
    cout << " THe max number is " << maxnumber(2.4, 2.6);
    cout << " THe max number is " << maxnumber('A', 'S');

    return 0;
}
